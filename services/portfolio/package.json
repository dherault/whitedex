{
  "private": true,
  "name": "pairs",
  "version": "0.0.0",
  "description": "Whitedex's portfolio service",
  "scripts": {
    "start": "node src/index.js",
    "start:watch": "nodemon --exec \"npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "dexters": "^0.6.0",
    "firebase-admin": "^10.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
